// Used in production builds (ng build --configuration production).
// Replace with your deployed backend URL before building for deployment.
export const environment = {
  production: true,
  apiUrl: 'https://YOUR-BACKEND-URL.example.com/api'
};
